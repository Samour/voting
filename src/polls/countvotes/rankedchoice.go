package countvotes

import "github.com/Samour/voting/polls/model"

func countRankedChoice(poll *model.Poll) {
}

// func loadVotes(pollId string) map[string]int {
// 	votes := make(map[string]int, 0)
// }
